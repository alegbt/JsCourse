
// number - all numbers (decimals too)
let age = 29;

// String - single or double quotes- 
let name = "ale";

// boolean - logical type
let isMale = true;

// Undefined - value for variables not yet defined
let aless;

// Null - empty value

// Symbol - unique and cannot be changed

// BigInt - larger than int can hold

// Js has dynamic typing, no need to define; js determines by itself - posso cambiare anche il value type di una variabile
let vari1 = 1;
vari1 = "abc";